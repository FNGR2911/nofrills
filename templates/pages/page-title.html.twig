{%
  set classes = [
    'page__title',
  ]
%}

<div class="container">
  {{ title_prefix }}
  {% if title %}
    <h1{{ title_attributes.addClass(classes) }}>{{ title }}</h1>
  {% endif %}
  {{ title_suffix }}
</div>
